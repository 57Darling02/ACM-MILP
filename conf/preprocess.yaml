defaults:
  - _self_
  - hydra: preprocess
  - dataset: mis
  - paths: common

seed: 2
num_workers: 10

compute_features: true
solve_instances: true

solver:
  mip_gap: 0.1  # 10% gap
  time_limit: 500.0 # seconds
  save_solution: false
  solution_dir: ./preprocess/${dataset.name}/solutions/